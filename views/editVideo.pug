extends layouts/main

block content
    .form-container
        form(action=routes.editVideo(video.id), method="post")
            input(type="text", placeholder="Title", name="제목", value=video.title)
            textarea(name="description", placeholder="설명")=video.description
            input(type="submit", value="비디오 수정하기")
        a.form-container__link.form-container__link--delete(href=routes.deleteVideo(video.id)) 비디오 삭제하기